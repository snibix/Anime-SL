import NavBar from "./NavBar";
// import PropTypes from 'prop-types'

function Header() {
  return (
    <header>
      <NavBar />
    </header>
  );
}

Header.propTypes = {};

export default Header;
